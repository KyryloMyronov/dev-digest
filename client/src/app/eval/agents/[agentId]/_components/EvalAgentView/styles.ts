import type { CSSProperties } from "react";

/** Co-located styles for /eval/agents/[agentId]. */
export const s = {
  page: { padding: "24px 32px 44px", maxWidth: 1100, margin: "0 auto" } satisfies CSSProperties,
  header: { display: "flex", alignItems: "center", gap: 14, marginBottom: 16 } satisfies CSSProperties,
  headerText: { flex: 1 } satisfies CSSProperties,
  h1: { fontSize: 22, fontWeight: 700, letterSpacing: "-0.02em" } satisfies CSSProperties,
  back: {
    background: "none",
    border: 0,
    padding: 0,
    cursor: "pointer",
    color: "var(--accent)",
    font: "inherit",
    fontSize: 13,
  } satisfies CSSProperties,
  alert: {
    display: "flex",
    gap: 10,
    alignItems: "flex-start",
    padding: "10px 12px",
    borderRadius: 8,
    border: "1px solid var(--crit)",
    background: "var(--crit-bg)",
    fontSize: 13,
    marginBottom: 16,
  } satisfies CSSProperties,
  alertLabel: { fontWeight: 700, color: "var(--crit)" } satisfies CSSProperties,
  tiles: {
    display: "grid",
    gridTemplateColumns: "repeat(3, minmax(0, 1fr))",
    gap: 12,
    marginBottom: 20,
  } satisfies CSSProperties,
  tile: {
    padding: 14,
    borderRadius: 8,
    border: "1px solid var(--border)",
    background: "var(--bg-elevated)",
  } satisfies CSSProperties,
  tileLabel: { fontSize: 11, letterSpacing: "0.06em", color: "var(--text-secondary)" } satisfies CSSProperties,
  tileValue: { fontSize: 26, fontWeight: 700, marginTop: 4 } satisfies CSSProperties,
  sectionLabel: {
    fontSize: 12,
    letterSpacing: "0.06em",
    color: "var(--text-secondary)",
    margin: "24px 0 8px",
  } satisfies CSSProperties,
  chartCard: {
    padding: 16,
    borderRadius: 8,
    border: "1px solid var(--border)",
    background: "var(--bg-elevated)",
  } satisfies CSSProperties,
  table: { width: "100%", borderCollapse: "collapse", fontSize: 13 } satisfies CSSProperties,
  th: {
    textAlign: "left",
    padding: "8px 10px",
    color: "var(--text-secondary)",
    fontWeight: 500,
    borderBottom: "1px solid var(--border)",
  } satisfies CSSProperties,
  td: { padding: "8px 10px", borderBottom: "1px solid var(--border)" } satisfies CSSProperties,
  compareBar: { display: "flex", alignItems: "center", gap: 12, margin: "12px 0" } satisfies CSSProperties,
  hint: { fontSize: 12, color: "var(--text-secondary)" } satisfies CSSProperties,
};
